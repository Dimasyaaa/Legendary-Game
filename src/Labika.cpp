#include "Menu.hpp"
#include "Labirint.hpp"

using namespace std;

int main() {
    SetConsoleCP(CP_UTF8);
    SetConsoleOutputCP(CP_UTF8);

    Menu menu;
    menu.displayMainMenu();

    return 0;
}